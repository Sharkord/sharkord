diff --git a/apps/client/src/components/left-sidebar/index.tsx b/apps/client/src/components/left-sidebar/index.tsx
index 845aac5..d0adbd4 100644
--- a/apps/client/src/components/left-sidebar/index.tsx
+++ b/apps/client/src/components/left-sidebar/index.tsx
@@ -11,6 +11,7 @@ import { memo } from 'react';
 import { Categories } from './categories';
 import { DirectMessages } from './direct-messages';
 import { DmButton } from './direct-messages/dm-button';
+import { PluginSidebarButtons } from './plugin-buttons';
 import { ServerDropdownMenu } from './server-dropdown';
 import { UserControl } from './user-control';
 import { VoiceControl } from './voice-control';
@@ -49,6 +50,7 @@ const LeftSidebar = memo(({ className }: TLeftSidebarProps) => {
         </div>
       </div>
       {publicSettings?.directMessagesEnabled && <DmButton />}
+      <PluginSidebarButtons />
       <div className="flex-1 overflow-y-auto">
         {dmsOpen ? <DirectMessages /> : <Categories />}
       </div>
diff --git a/apps/client/src/components/left-sidebar/plugin-buttons.tsx b/apps/client/src/components/left-sidebar/plugin-buttons.tsx
new file mode 100644
index 0000000..751d6da
--- /dev/null
+++ b/apps/client/src/components/left-sidebar/plugin-buttons.tsx
@@ -0,0 +1,50 @@
+import { setActivePluginId } from '@/features/app/actions';
+import { useActivePluginId } from '@/features/app/hooks';
+import { usePluginComponentsBySlot } from '@/features/server/plugins/hooks';
+import { PluginSlot } from '@sharkord/shared';
+import { cn, Tooltip } from '@sharkord/ui';
+import { PuzzleIcon, X } from 'lucide-react';
+import { memo, useMemo } from 'react';
+
+export const PluginSidebarButtons = memo(() => {
+  const componentsMap = usePluginComponentsBySlot(PluginSlot.FULL_SCREEN);
+  const activePluginId = useActivePluginId();
+
+  const sidebarPluginIds = useMemo(() => {
+    return Object.keys(componentsMap);
+  }, [componentsMap]);
+
+  if (sidebarPluginIds.length === 0) return null;
+
+  return (
+    <div className="border-b border-border px-2 py-2">
+      {sidebarPluginIds.map((pluginId) => {
+        const isActive = activePluginId === pluginId;
+
+        return (
+          <Tooltip
+            key={pluginId}
+            content={`${isActive ? 'Close' : 'Open'} ${pluginId}`}
+          >
+            <button
+              type="button"
+              onClick={() => {
+                const nextId = isActive ? undefined : pluginId;
+                setActivePluginId(nextId);
+              }}
+              className={cn(
+                'flex w-full items-center gap-2 rounded px-2 py-1.5 text-sm text-muted-foreground hover:bg-accent hover:text-accent-foreground',
+                isActive &&
+                  'bg-accent text-accent-foreground ring-1 ring-primary/30'
+              )}
+            >
+              <PuzzleIcon className="h-4 w-4" />
+              <span className="flex-1 text-left truncate">{pluginId}</span>
+              {isActive && <X className="h-4 w-4" />}
+            </button>
+          </Tooltip>
+        );
+      })}
+    </div>
+  );
+});
diff --git a/apps/client/src/components/plugin-slot-renderer/index.tsx b/apps/client/src/components/plugin-slot-renderer/index.tsx
index 9ff5af2..c215f73 100644
--- a/apps/client/src/components/plugin-slot-renderer/index.tsx
+++ b/apps/client/src/components/plugin-slot-renderer/index.tsx
@@ -19,15 +19,20 @@ const SlotContextProvider = memo(({ children }: TSlotContextProviderProps) => {
 type TPluginSlotRendererProps = {
   slotId: PluginSlot;
   debug?: boolean;
+  activePluginId?: string;
 };
 
 const PluginSlotRenderer = memo(
-  ({ slotId, debug = isDebug() }: TPluginSlotRendererProps) => {
+  ({ slotId, debug = isDebug(), activePluginId }: TPluginSlotRendererProps) => {
     const pluginComponentsBySlot = usePluginComponentsBySlot(slotId);
 
     const content = Object.entries(pluginComponentsBySlot).map(
-      ([pluginId, components]) =>
-        components.map((Component, index) => {
+      ([pluginId, components]) => {
+        if (activePluginId && pluginId !== activePluginId) {
+          return null;
+        }
+
+        return components.map((Component, index) => {
           const content = (
             <SlotContextProvider>
               {(ctx) => <Component {...ctx} />}
@@ -51,7 +56,8 @@ const PluginSlotRenderer = memo(
               {wrappedContent}
             </ErrorBoundary>
           );
-        })
+        });
+      }
     );
 
     return <>{content}</>;
diff --git a/apps/client/src/features/app/actions.ts b/apps/client/src/features/app/actions.ts
index 13cf6f1..2923208 100644
--- a/apps/client/src/features/app/actions.ts
+++ b/apps/client/src/features/app/actions.ts
@@ -137,3 +137,6 @@ export const setBrowserNotificationsForDms = async (enabled: boolean) => {
     enabled
   );
 };
+
+export const setActivePluginId = (pluginId: string | undefined) =>
+  store.dispatch(appSliceActions.setActivePluginId(pluginId));
diff --git a/apps/client/src/features/app/hooks.ts b/apps/client/src/features/app/hooks.ts
index a716b9c..199e564 100644
--- a/apps/client/src/features/app/hooks.ts
+++ b/apps/client/src/features/app/hooks.ts
@@ -1,6 +1,7 @@
 import { useMemo } from 'react';
 import { useSelector } from 'react-redux';
 import {
+  activePluginIdSelector,
   appLoadingSelector,
   autoJoinLastChannelSelector,
   browserNotificationsForDmsSelector,
@@ -49,3 +50,5 @@ export const useBrowserNotificationsForMentions = () =>
 
 export const useBrowserNotificationsForDms = () =>
   useSelector(browserNotificationsForDmsSelector);
+
+export const useActivePluginId = () => useSelector(activePluginIdSelector);
diff --git a/apps/client/src/features/app/selectors.ts b/apps/client/src/features/app/selectors.ts
index 18ea130..f8586a8 100644
--- a/apps/client/src/features/app/selectors.ts
+++ b/apps/client/src/features/app/selectors.ts
@@ -42,6 +42,9 @@ export const browserNotificationsForMentionsSelector = (state: IRootState) =>
 export const browserNotificationsForDmsSelector = (state: IRootState) =>
   state.app.browserNotificationsForDms;
 
+export const activePluginIdSelector = (state: IRootState) =>
+  state.app.activePluginId;
+
 export const threadSidebarDataSelector = createSelector(
   [
     threadSidebarOpenSelector,
diff --git a/apps/client/src/features/app/slice.ts b/apps/client/src/features/app/slice.ts
index a73487e..5ba27ce 100644
--- a/apps/client/src/features/app/slice.ts
+++ b/apps/client/src/features/app/slice.ts
@@ -18,6 +18,7 @@ export interface TAppState {
   browserNotifications: boolean;
   browserNotificationsForMentions: boolean;
   browserNotificationsForDms: boolean;
+  activePluginId: string | undefined;
 }
 
 const initialState: TAppState = {
@@ -47,7 +48,8 @@ const initialState: TAppState = {
   browserNotificationsForDms: getLocalStorageItemBool(
     LocalStorageKey.BROWSER_NOTIFICATIONS_FOR_DMS,
     false
-  )
+  ),
+  activePluginId: undefined
 };
 
 export const appSlice = createSlice({
@@ -111,6 +113,12 @@ export const appSlice = createSlice({
     },
     setBrowserNotificationsForDms: (state, action: PayloadAction<boolean>) => {
       state.browserNotificationsForDms = action.payload;
+    },
+    setActivePluginId: (state, action: PayloadAction<string | undefined>) => {
+      state.activePluginId = action.payload;
+      if (action.payload) {
+        state.dmsOpen = false;
+      }
     }
   }
 });
diff --git a/apps/client/src/screens/server-view/content-wrapper.tsx b/apps/client/src/screens/server-view/content-wrapper.tsx
index fdac497..995d23e 100644
--- a/apps/client/src/screens/server-view/content-wrapper.tsx
+++ b/apps/client/src/screens/server-view/content-wrapper.tsx
@@ -1,6 +1,7 @@
 import { TextChannel } from '@/components/channel-view/text';
 import { VoiceChannel } from '@/components/channel-view/voice';
 import { PluginSlotRenderer } from '@/components/plugin-slot-renderer';
+import { useActivePluginId } from '@/features/app/hooks';
 import {
   useSelectedChannelId,
   useSelectedChannelType
@@ -21,6 +22,20 @@ const ContentWrapper = memo(
     const selectedChannelId = useSelectedChannelId();
     const selectedChannelType = useSelectedChannelType();
     const serverName = useServerName();
+    const activePluginId = useActivePluginId();
+
+    if (activePluginId) {
+      return (
+        <main className="flex flex-1 flex-col bg-background relative min-w-0 min-h-0">
+          <div className="flex-col gap-2 h-full w-full flex overflow-auto relative bg-background">
+            <PluginSlotRenderer
+              slotId={PluginSlot.FULL_SCREEN}
+              activePluginId={activePluginId}
+            />
+          </div>
+        </main>
+      );
+    }
 
     let content;
 
diff --git a/apps/server/src/routers/messages/events.ts b/apps/server/src/routers/messages/events.ts
index 30ac9f3..c690735 100644
--- a/apps/server/src/routers/messages/events.ts
+++ b/apps/server/src/routers/messages/events.ts
@@ -25,7 +25,10 @@ const onMessageTypingRoute = protectedProcedure.subscription(
 
 const onThreadReplyCountUpdateRoute = protectedProcedure.subscription(
   async ({ ctx }) => {
-    return ctx.pubsub.subscribe(ServerEvents.THREAD_REPLY_COUNT_UPDATE);
+    return ctx.pubsub.subscribeFor(
+      ctx.userId,
+      ServerEvents.THREAD_REPLY_COUNT_UPDATE
+    );
   }
 );
 
diff --git a/packages/shared/src/plugins.ts b/packages/shared/src/plugins.ts
index 2864501..53a294a 100644
--- a/packages/shared/src/plugins.ts
+++ b/packages/shared/src/plugins.ts
@@ -14,7 +14,13 @@ export const zPluginPackageJson = z.object({
     author: z.string().min(1, 'Plugin author is required'),
     homepage: z.url().optional(),
     description: z.string().min(1, 'Plugin description is required'),
-    logo: z.string().optional()
+    logo: z.string().optional(),
+    sidebar: z
+      .object({
+        icon: z.string().min(1, 'Sidebar icon (SVG string) is required'),
+        title: z.string().min(1, 'Sidebar title is required')
+      })
+      .optional()
   })
 });
 
@@ -132,7 +138,8 @@ export enum PluginSlot {
   CONNECT_SCREEN = 'connect_screen',
   HOME_SCREEN = 'home_screen',
   CHAT_ACTIONS = 'chat_actions',
-  TOPBAR_RIGHT = 'topbar_right'
+  TOPBAR_RIGHT = 'topbar_right',
+  FULL_SCREEN = 'full_screen'
 }
 
 export type TPluginComponentsMapBySlotIdMapListByPlugin = {
